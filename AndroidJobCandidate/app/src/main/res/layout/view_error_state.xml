<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="app.storytel.candidate.com.post.domain.data.remote.repository.resource.Resource" />

        <import type="app.storytel.candidate.com.post.domain.data.remote.repository.resource.Resource.Status" />

        <variable
            name="resource"
            type="Resource" />

        <variable
            name="callback"
            type="app.storytel.candidate.com.common.RetryCallback" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/dimen_16"
        app:visibleGone="@{resource.status == Status.ERROR}">

        <Button
            android:id="@+id/retry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button_selector"
            android:onClick="@{() -> callback.retry()}"
            android:paddingStart="@dimen/dimen_20"
            android:paddingEnd="@dimen/dimen_20"
            android:text="@string/error_text_retry"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="@dimen/size_17" />

        <TextView
            android:id="@+id/message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dimen_20"
            android:text="@{resource.error ?? @string/error_unknown}"
            android:textSize="@dimen/size_17"
            app:visibleGone="@{resource.status == Status.ERROR}"
            tools:text="Error message" />
    </LinearLayout>
</layout>